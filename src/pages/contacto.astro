---
import Layout from "../layouts/layout.astro";
import Navigation from "../components/ui/Navigation/Navigation.astro";
import Breadcrumb from "../components/ui/Breadcrumb/Breadcrumb.astro";
import PageHeroSection from "../components/HeroSection/PageHeroSection.astro";
import ArrowLink from "../components/ui/Button/ArrowLink.astro";
import type { BreadcrumbItem } from "../types";

const breadcrumbItems: BreadcrumbItem[] = [
  {
    label: "Contacto",
    href: "/contacto",
    active: true,
  },
];
---

<Layout title="Contacto | MUGA" description="Iniciá tu proyecto con KPI garantizados">
  <Navigation>
    <Breadcrumb items={breadcrumbItems} />
  </Navigation>
  <div class="hidden lg:block">
    <Breadcrumb items={breadcrumbItems} />
  </div>
  <PageHeroSection
    title="Iniciá tu proyecto"
    description="Contanos tu idea. Construimos con método y entregamos con valor."
    textAlign="left"
    showBadge={true}
    badgeText="AWNG"
  />

  <section class="relative px-6 lg:px-8 py-24">
    <div class="mx-auto max-w-7xl">
      <div class="mb-16">
        <div class="text-center mb-10">
          <h2 class="text-2xl font-light text-white mb-3">Proceso</h2>
          <p class="text-white/75 max-w-2xl mx-auto">
            Tres etapas claras para convertir tu idea en un proyecto ejecutable y medible.
          </p>
        </div>

        <ol class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <li class="h-full min-h-[290px] bg-muga-dark-800/40 border border-white/10 p-6 flex flex-col transition-colors duration-200 hover:border-accent/25">
            <p class="text-accent text-xs font-mono font-semibold tracking-wider mb-3">01</p>
            <h3 class="text-white text-lg font-medium mb-2">Descubrimiento</h3>
            <p class="text-white/75 mb-7 flex-1">Alineamos objetivos de negocio, alcance y prioridades del proyecto.</p>
            <div class="space-y-2 text-sm">
              <p><span class="text-accent font-mono uppercase text-[11px] tracking-wider">Tiempo:</span> <span class="text-white">30-45 min</span></p>
              <p><span class="text-accent font-mono uppercase text-[11px] tracking-wider">Entregable:</span> <span class="text-white">Diagnóstico inicial</span></p>
            </div>
          </li>

          <li class="h-full min-h-[290px] bg-muga-dark-800/40 border border-white/10 p-6 flex flex-col transition-colors duration-200 hover:border-accent/25">
            <p class="text-accent text-xs font-mono font-semibold tracking-wider mb-3">02</p>
            <h3 class="text-white text-lg font-medium mb-2">Arquitectura y KPIs</h3>
            <p class="text-white/75 mb-7 flex-1">Definimos stack, roadmap técnico y objetivos medibles por etapa.</p>
            <div class="space-y-2 text-sm">
              <p><span class="text-accent font-mono uppercase text-[11px] tracking-wider">Tiempo:</span> <span class="text-white">24-48 h</span></p>
              <p><span class="text-accent font-mono uppercase text-[11px] tracking-wider">Entregable:</span> <span class="text-white">Propuesta + roadmap</span></p>
            </div>
          </li>

          <li class="h-full min-h-[290px] bg-muga-dark-800/40 border border-white/10 p-6 flex flex-col transition-colors duration-200 hover:border-accent/25">
            <p class="text-accent text-xs font-mono font-semibold tracking-wider mb-3">03</p>
            <h3 class="text-white text-lg font-medium mb-2">Ejecución y seguimiento</h3>
            <p class="text-white/75 mb-7 flex-1">Implementamos por iteraciones con reportes transparentes y control continuo.</p>
            <div class="space-y-2 text-sm">
              <p><span class="text-accent font-mono uppercase text-[11px] tracking-wider">Tiempo:</span> <span class="text-white">Iterativo</span></p>
              <p><span class="text-accent font-mono uppercase text-[11px] tracking-wider">Entregable:</span> <span class="text-white">Entrega + reporte KPI</span></p>
            </div>
          </li>
        </ol>
      </div>

      <!-- Formulario de contacto -->
      <div class="bg-muga-dark-800/50 border border-white/5 p-8">
        <!-- Mensajes de estado del formulario -->
        <div id="form-messages" class="mb-6 hidden">
          <div id="success-message" class="hidden bg-green-500/10 border border-green-500/20 text-green-400 px-4 py-3">
            ¡Mensaje enviado exitosamente! Nos pondremos en contacto pronto.
          </div>
          <div id="error-message" class="hidden bg-red-500/10 border border-red-500/20 text-red-400 px-4 py-3">
            Error al enviar el mensaje. Por favor, intenta de nuevo.
          </div>
        </div>

        <form
          id="contact-form"
          method="POST"
          action="https://hook.us2.make.com/d2244ejjygfwjirzirv7xg5j8olpv8cd"
          target="make-webhook-target"
          accept-charset="UTF-8"
          class="space-y-6"
        >
          <input type="hidden" name="created_at" value="" />
          <input type="hidden" name="success_url" value="/contacto/enviado" />
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <label for="name" class="block text-sm font-medium text-white mb-2">Nombre</label>
              <input
                type="text"
                id="name"
                name="name"
                class="w-full px-3 py-2 bg-muga-dark-800/70 border border-white/10 text-white placeholder-graylight focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent"
                placeholder="Tu nombre"
                required
              />
            </div>
            <div>
              <label for="email" class="block text-sm font-medium text-white mb-2">Email</label>
              <input
                type="email"
                id="email"
                name="email"
                class="w-full px-3 py-2 bg-muga-dark-800/70 border border-white/10 text-white placeholder-graylight focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent"
                placeholder="tu@email.com"
                required
              />
            </div>
          </div>

          <div>
            <label for="phone" class="block text-sm font-medium text-white mb-2">Teléfono</label>
            <input
              type="tel"
              id="phone"
              name="phone"
              class="w-full px-3 py-2 bg-muga-dark-800/70 border border-white/10 text-white placeholder-graylight focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent"
              placeholder="+54 9 11 1234-5678"
              autocomplete="tel"
              inputmode="tel"
            />
          </div>

          <div>
            <label for="project" class="block text-sm font-medium text-white mb-2">Tipo de proyecto</label>
            <select
              id="project"
              name="project"
              class="w-full px-3 py-2 bg-muga-dark-800/70 border border-white/10 text-white focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent"
              required
            >
              <option value="">Seleccioná una opción</option>
              <option value="landing">Landing Page</option>
              <option value="corporativo">Sitio Corporativo</option>
              <option value="institucional">Sitio Institucional</option>
            </select>
          </div>

          <div>
            <label for="message" class="block text-sm font-medium text-white mb-2">Mensaje</label>
            <textarea
              id="message"
              name="message"
              rows="4"
              class="w-full px-3 py-2 bg-muga-dark-800/70 border border-white/10 text-white placeholder-graylight focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent"
              placeholder="Contanos sobre tu proyecto..."
            ></textarea>
          </div>

          <input type="hidden" name="page" value="" />
          <input type="hidden" name="source" value="website" />
          <input type="hidden" name="status" value="new" />
          <input type="hidden" name="ip" value="" />

          <div class="text-center">
            <button
              type="submit"
              id="submit-btn"
              class="inline-flex items-center gap-2 px-6 py-3 bg-accent text-obscure font-semibold hover:bg-accent/90 focus:outline-none focus:ring-2 focus:ring-accent focus:ring-offset-2 focus:ring-offset-obscure transition disabled:opacity-50 disabled:cursor-not-allowed"
            >
              <span id="btn-text">Enviar consulta</span>
              <span id="btn-arrow" aria-hidden="true">→</span>
              <div id="btn-spinner" class="hidden animate-spin rounded-full h-4 w-4 border-b-2 border-obscure" aria-hidden="true"></div>
            </button>
          </div>
        </form>
        <iframe id="make-webhook-target" name="make-webhook-target" class="hidden" aria-hidden="true"></iframe>
      </div>

      <div class="text-center mt-12">
        <p class="text-muga-neutral mb-4">O explorá nuestros productos</p>
        <div class="flex justify-center gap-6">
          <ArrowLink href="/productos" text="Productos" />
          <ArrowLink href="/portafolio" text="Portafolio" />
        </div>
      </div>
    </div>
  </section>

  <style>
    /* Estilos para el select */
    select {
      background-color: rgba(24, 23, 23, 0.7) !important;
      color: #A0A0A0 !important;
    }

    select:focus {
      color: white !important;
    }

    select option {
      background-color: #191717 !important;
      color: white !important;
    }
  </style>

  <script src="../scripts/contact-form-redirect.ts"></script>

</Layout>
